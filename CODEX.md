# Codex Instructions

Read `AGENTS.md` first. It is the canonical agent handoff for this repository.

Then read `REPO_MAP.md`, `README.md`, and the active sprint prompt under `docs/sprints/` before changing files.

Keep work scoped, preserve privacy/export boundaries, and run:

```bash
python3 -m pytest -q
```

before reporting completion.
